[project]
name = "silicai"
version = "0.1.0"
description = "Structured electronic component descriptions for AI-assisted circuit design"
requires-python = ">=3.11"
dependencies = [
    "pyyaml",
    "jsonschema",
    "referencing",
]

[project.scripts]
silicai-validate = "tools.validate:main"
