{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mageoch/silicai/schema/defs/circuit_power_rail.schema.json",
  "title": "Circuit Power Rail",
  "description": "A power rail instantiated in a circuit, referencing a global net",
  "type": "object",
  "required": ["net"],
  "properties": {
    "net": { "type": "string", "description": "Net name matching a global net definition (e.g. VCC_3V3, GND)" }
  },
  "additionalProperties": false
}
