{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mageoch.github.io/silicai/schema/defs/relative_threshold.schema.json",
  "title": "Relative Threshold",
  "description": "A voltage threshold expressed as a fraction of a reference voltage (e.g. 0.7 × V+)",
  "type": "object",
  "required": ["multiplier", "reference"],
  "properties": {
    "multiplier": { "type": "number", "description": "Fraction of the reference voltage (e.g. 0.7 for 70%)" },
    "reference":  { "type": "string", "description": "Reference voltage identifier (e.g. V+, VCC, VDD)" }
  },
  "additionalProperties": false
}
