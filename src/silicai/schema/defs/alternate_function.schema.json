{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mageoch/silicai/schema/defs/alternate_function.schema.json",
  "title": "Alternate Pin Function",
  "description": "An alternate peripheral function available on a GPIO pin",
  "type": "object",
  "required": ["peripheral", "signal", "direction"],
  "properties": {
    "af":         { "description": "Alternate function number (microcontroller-specific, e.g. AF4 for STM32)" },
    "peripheral": { "type": "string", "description": "Peripheral name (e.g. I2C1, SPI2, USART3)" },
    "signal":     { "type": "string", "description": "Signal name within the peripheral (e.g. SCL, MOSI, TX)" },
    "direction": {
      "type": "string",
      "description": "Signal direction when this alternate function is active",
      "enum": ["input", "output", "bidirectional"]
    }
  }
}
