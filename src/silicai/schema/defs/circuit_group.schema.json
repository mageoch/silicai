{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mageoch.github.io/silicai/schema/defs/circuit_group.schema.json",
  "title": "Circuit Group",
  "description": "A named group of component instances sharing a common purpose, nested inline within the instances array",
  "type": "object",
  "required": ["group", "instances"],
  "properties": {
    "group": { "type": "string", "description": "Short identifier for this group (e.g. shield_protection)" },
    "description": { "type": "string", "description": "Human-readable description of the group's purpose" },
    "instances": {
      "type": "array",
      "description": "Component instances belonging to this group",
      "items": { "$ref": "circuit_instance.schema.json" },
      "minItems": 1
    }
  },
  "additionalProperties": false
}
